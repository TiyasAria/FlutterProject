import 'dart:html';

import 'package:flutter/material.dart';

class TimeSholat extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: <Widget>[
          Container(
            child: Text('Alarm Waktu Sholat', style: ,),
          )
        ],
      ),
    );
  }
}
